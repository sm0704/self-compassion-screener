# LOCAL TESTING ONLY.
# Copy this file to "secrets.toml" (which is gitignored) and fill in the values.
#
# On Streamlit Community Cloud, do NOT commit secrets — paste these two lines into the
# app's  Settings → Secrets  panel instead.

GOOGLE_API_KEY = "AQ.paste-your-google-ai-studio-key-here"
APP_PASSWORD   = "choose-a-password-for-your-users"
